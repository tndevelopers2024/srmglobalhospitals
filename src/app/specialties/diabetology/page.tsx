import "@/styles/specialty.css";

import { createMetadata } from "@/lib/seo";
import { breadcrumbSchema, jsonLdScriptProps } from "@/lib/structured-data";
import { HeaderTop, SiteHeader, SiteFooter } from "@/components/layout";
import ArtBody from "./ArtBody";

export const metadata = createMetadata({
  title: "Best Diabetology Hospital in Chengalpattu | SRM Global Hospitals",
  description: "Recognized as the best diabetology hospital in Chengalpattu, our diabetes specialists and diabetologists provide comprehensive diabetes treatment, including type 2 diabetes management and insulin therapy, along with dedicated diabetic foot care and gestational diabetes treatment.",
  path: "/specialties/diabetology",
});

export default function DiabetologyPage() {
  return (
    <>
      <script
        type="application/ld+json"
        {...jsonLdScriptProps(
          breadcrumbSchema([
            { name: "Home", path: "/" },
            { name: "Our Specialties", path: "/#specialties" },
            { name: "Department of Diabetology", path: "/specialties/diabetology" },
          ]),
        )}
      />
      <HeaderTop />
      <SiteHeader activeNav="specialties" />
      <main className="specialty-page" id="main-content">
        <ArtBody />
      </main>
      <SiteFooter />
    </>
  );
}
